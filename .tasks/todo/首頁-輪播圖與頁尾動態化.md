# 簡易任務 (Task - Simple)

**請逐一執行以下步驟，每執行一步，就標記為完成，直到所有步驟都完成。完成執行清單之後也請進行驗證，逐一進行驗證，直到所有驗證都通過。完成後，請在第一大標題後面加上「 - 現在日期時間」的格式。**

## 1. 任務目標 (Objective)
- **目的**：將首頁的輪播圖 (Carousel) 改為讀取後台設定，並將頁尾年份改為自動抓取，確保頁面資訊與系統同步。
- **涉及檔案**：`microlink-template-space-rental/index.php`

## 2. 執行清單 (Action Items)
- [ ] 確認後台傳遞至前端的輪播圖資料變數 (例如 `$banners` 或 `$setting->carousel` 等)。
- [ ] 修改 `index.php` 的 `#mainCarousel` 區塊，移除靜態 `carousel-item`。
- [ ] 使用 `foreach` 迴圈動態輸出輪播圖片，確保第一張圖有 `active` class。
- [ ] 修改 `index.php` 頁尾 (Footer)，將寫死的年份 `2025` 改為 PHP 動態輸出 `<?php echo date("Y"); ?>`。

## 3. 驗證重點 (Verification)
- [ ] 首頁輪播圖能正常顯示後台設定的圖片，且輪播功能正常。
- [ ] 頁尾版權宣告顯示當前正確年份。
