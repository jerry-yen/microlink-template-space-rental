# 簡易任務 (Task - Simple)

**請逐一執行以下步驟，每執行一步，就標記為完成，直到所有步驟都完成。完成執行清單之後也請進行驗證，逐一進行驗證，直到所有驗證都通過。完成後，請在第一大標題後面加上「 - 現在日期時間」的格式。**

## 1. 任務目標 (Objective)
- **目的**：將首頁「思辨空間簡介」區塊的靜態內容 (標題、簡介、特色列點、右側圖片) 改為由後台資料動態產生。
- **涉及檔案**：`microlink-template-space-rental/index.php`

## 2. 執行清單 (Action Items)
- [ ] 確認後台傳遞的簡介相關變數 (例如 `$setting->intro_title`, `$setting->intro_desc` 等) 及形象圖片路徑。
- [ ] 修改 `index.php` 的 `#thinking-space` 區塊，將標題與段落文字替換為 PHP 變數輸出。
- [ ] 若後台有提供簡介條列式重點 (Features)，將靜態 `<ul class="thinking-features">` 改為迴圈輸出；若無則確認是否需保留或隱藏。
- [ ] 將右側靜態圖片 (`img.thinking-img`) 的 `src` 改為後台設定的圖片路徑。

## 3. 驗證重點 (Verification)
- [ ] 簡介區塊的標題與內文正確顯示後台設定的內容。
- [ ] 右側形象圖片正常載入，無破圖情形。
